---
summary: "Adopt @cyanheads/mcp-ts-core 0.9.21 — per-request log context fix, secret scrubbing from error messages, withRetry fail-fast on non-retryable errors"
breaking: false
security: false
---

# 0.1.11 — 2026-06-02

## Changed

- **`@cyanheads/mcp-ts-core`** `^0.9.16` → `^0.9.21`: per-request log context fix (per-request logs and traces carry fresh request + trace/span IDs instead of frozen boot context); `fetchWithTimeout` strips query-string secrets (e.g. `?api_key=`) from error messages and logs; `withRetry` fails fast on non-retryable errors; `ctx.fail` auto-populates the `retryable` flag.
- Re-synced skills and scripts to framework 0.9.21 (`add-service`, `add-tool`, `api-canvas`, `api-context`, `api-linter`, `api-utils`, `design-mcp-server`, `release-and-publish`); added `api-mirror` and `orchestrations` skills; added `scripts/check-skill-versions.ts` and `scripts/release-github.ts`.
- README client-config key renamed to full package name (`noaa-cdo-mcp-server`).

## Dependencies

- `@cyanheads/mcp-ts-core` ^0.9.16 → ^0.9.21
- `vitest` ^4.1.7 → ^4.1.8
