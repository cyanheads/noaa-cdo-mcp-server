---
summary: "code simplification, mcp-ts-core ^0.9.7 → ^0.9.9, error code corrections"
breaking: false
security: false
---

# 0.1.8 — 2026-05-24

## Changed

- **mcp-ts-core** ^0.9.7 → ^0.9.9 — picks up fuzzTool Phase 1 pre-parse fix, `ZodArray` minLength propagation, `fast-check` moved to optional peer dep, `git-wrapup` skill, and README/skill doc updates.
- **Null coalescing** — `value == null` guards and `??` operators replace verbose `=== null || === undefined` chains in `cdo-service.ts` and tool files.
- **Template literal** for server instructions in `src/index.ts`; eliminated duplicate `qs.toString()` call in `cdo-service.ts`.
- **Conditional spread** normalization across 5 tool files — consistent pattern for optional query parameter injection.
- **Error codes** corrected at 3 sites: `date_range_exceeded` and `invalid_params` in `noaa-fetch-data.tool.ts` and `noaa-list-datasets.tool.ts` changed from `InvalidParams` to `ValidationError`.
- **Skills synced** — `git-wrapup` v1.0 added; `release-and-publish` updated to v2.6.
- **CLAUDE.md** updated: framework version reference `^0.9.7` → `^0.9.9`; "Close the loop on issues" rule added.
